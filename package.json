{
  "name": "mares-e-mares",
  "private": true,
  "type": "module",
  "description": "Site da campanha Mares e Marés — utilitários de linha de comando",
  "scripts": {
    "pull": "node scripts/pull.mjs",
    "test:sync": "node scripts/sync-test.mjs"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  }
}
